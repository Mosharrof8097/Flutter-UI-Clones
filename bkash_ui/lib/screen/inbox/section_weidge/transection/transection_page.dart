import 'package:flutter/material.dart';
class TransectionPage extends StatelessWidget {
  const TransectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("No notification"),

    );
  }
}